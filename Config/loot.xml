<configs>

    <!-- Bird nest: about 2× feathers -->
    <set
        xpath="/lootcontainers/lootcontainer[item[@name='resourceFeather'] and item[contains(@name,'Egg')]]/item[@name='resourceFeather']/@count">
        8,22</set>

    <!-- Bird nest: eggs more often -->
    <set
        xpath="/lootcontainers/lootcontainer[item[@name='resourceFeather'] and item[contains(@name,'Egg')]]/item[contains(@name,'Egg')]/@prob">
        0.65</set>

    <!-- Optional: a bit more eggs when they do drop -->
    <set
        xpath="/lootcontainers/lootcontainer[item[@name='resourceFeather'] and item[contains(@name,'Egg')]]/item[contains(@name,'Egg')]/@count">
        1,3</set>

</configs>